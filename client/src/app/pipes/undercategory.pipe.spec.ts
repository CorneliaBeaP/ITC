import { UndercategoryPipe } from './undercategory.pipe';

describe('UndercategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new UndercategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
