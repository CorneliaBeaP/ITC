import { AttributesAlphabeticalPipe } from './attributes-alphabetical.pipe';

describe('AttributesAlphabeticalPipe', () => {
  it('create an instance', () => {
    const pipe = new AttributesAlphabeticalPipe();
    expect(pipe).toBeTruthy();
  });
});
